apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  
data:
  APP_ENV: "production"
  APP_PORT: "8080"

  config.properties: |
    server.port=8080
    server.debug=false
    db.host=mysql-service